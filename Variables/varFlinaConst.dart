void main(){
  // var/final/ const variableName = value;

  var someValue = 10;
  final someValue2 = 10;// The final variable 'someValue2' can only be set once
  const someValue3 = 10; //Constant variables can't be assigned a value

  // print(someValue);
  // print(someValue2);
  // print(someValue3);

  print('-------------------');
  someValue = 100;
  // someValue2 = 1000;
  // someValue3 = 1;


// Var is Mutable means --> The value can be changed after it's Set!!!

// Final && const is immutable means --> the value can't be set after it's set!!!
}